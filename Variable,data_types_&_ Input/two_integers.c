#include<stdio.h>
int main(){
    int first,second;
    printf("Enter first number: ");
    scanf("%d",&first);
    printf("Enter second number: ");
    scanf("%d", &second);
    printf("The two numbers are: %d and %d", first,second);

    return 0;
}